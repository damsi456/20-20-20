# 20-20-20
An eye care time that follows 20-20-20 rule. (Every 20 minutes, look at something 20 feet away for 20 seconds)

If you want to modify the styles:

```bash
npm install
npx tailwindcss -i input.css -o output.css --watch
```

